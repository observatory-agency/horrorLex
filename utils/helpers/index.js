const DataImport = require('./DataImport');
const handleCollection = require('./handleCollection');
const slugify = require('./slugify');
const stopWordRemover = require('./stopWordRemover');

module.exports = {
  DataImport,
  handleCollection,
  slugify,
  stopWordRemover,
};
